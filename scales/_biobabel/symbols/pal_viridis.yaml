# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.pal_viridis.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.pal_viridis
import_path: scales.pal_viridis
kind: function
signature: 'pal_viridis(alpha: ''float'' = 1, begin: ''float'' = 0, end: ''float''
  = 1, direction: ''int'' = 1, option: ''str'' = ''D'') -> ''DiscretePalette'''
purpose: Viridis family colour palettes.
description: Viridis family colour palettes.
parameters:
- name: alpha
  type: float
  required: false
  default: 1
- name: begin
  type: float
  required: false
  default: 0
- name: end
  type: float
  required: false
  default: 1
- name: direction
  type: int
  required: false
  default: 1
- name: option
  type: str
  required: false
  default: D
mutates: ''
returns: DiscretePalette
requires: []
writes: []
examples:
- "pal_viridis(option='D')(5)  # → ['#440154', ..., '#fde725']  (factory → call with integer n)"
failure_fixes:
- when: "TypeError: 'float' object cannot be interpreted as an integer"
  suggest:
  - scales.pal_seq_gradient
  - scales.pal_gradient_n
  explanation: pal_viridis returns a DISCRETE palette whose callable takes an integer count n, not a position. pal_viridis()(0.5) raises because the float flows into np.linspace(num=...). For a single color at a continuous position, wrap discrete samples with pal_gradient_n(pal_viridis()(6))(0.5), or use pal_seq_gradient.
related:
- scales.brewer_pal
- scales.pal_gradient_n
- scales.pal_hue
