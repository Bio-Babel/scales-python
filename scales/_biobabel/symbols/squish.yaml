# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.squish.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.squish
import_path: scales.squish
kind: function
signature: 'squish(x: ''Union[np.ndarray, list, float]'', range: ''_RangeLike'' =
  (0, 1), only_finite: ''bool'' = True) -> ''np.ndarray'''
purpose: Clamp (squish) values outside *range* to the nearest boundary.
description: Clamp (squish) values outside *range* to the nearest boundary.
parameters:
- name: x
  type: Union[np.ndarray, list, float]
  required: true
  default: null
- name: range
  type: _RangeLike
  required: false
  default: (0, 1)
- name: only_finite
  type: bool
  required: false
  default: true
mutates: ''
returns: np.ndarray
requires: []
writes: []
examples:
- "squish([-1, 0.5, 2], (0, 1))  # → array([0. , 0.5, 1. ])  (out-of-range clamped to limits)"
related:
- scales.censor
- scales.oob_squish
- scales.rescale
