# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.breaks_log.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.breaks_log
import_path: scales.breaks_log
kind: function
signature: 'breaks_log(n: ''int'' = 5, base: ''float'' = 10) -> ''Callable[[ArrayLike],
  np.ndarray]'''
purpose: Create a break generator for log-scaled axes.
description: Create a break generator for log-scaled axes.
parameters:
- name: n
  type: int
  required: false
  default: 5
- name: base
  type: float
  required: false
  default: 10
mutates: ''
returns: Callable[[ArrayLike], np.ndarray]
requires: []
writes: []
examples:
- "breaks_log(n=5, base=10)([1, 10000])  # → array([1., 10., 100., 1000., 10000.])  (factory → callable → ticks)"
failure_fixes:
- when: breaks_log returns an empty array.
  suggest:
  - scales.breaks_extended
  - scales.pseudo_log_trans
  explanation: breaks_log keeps only finite positive inputs; a range with no positive values yields no breaks. Pass the raw (pre-log) positive data range, or use breaks_extended for a non-log axis.
related:
- scales.log10_trans
- scales.label_log
- scales.minor_breaks_log
