# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.minor_breaks_width.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.minor_breaks_width
import_path: scales.minor_breaks_width
kind: function
signature: 'minor_breaks_width(width: ''float'', offset: ''float'' = 0) -> ''Callable[[np.ndarray,
  np.ndarray], np.ndarray]'''
purpose: Minor breaks at a fixed width per segment.
description: Minor breaks at a fixed width per segment.
parameters:
- name: width
  type: float
  required: true
  default: null
- name: offset
  type: float
  required: false
  default: 0
mutates: ''
returns: Callable[[np.ndarray, np.ndarray], np.ndarray]
requires: []
writes: []
examples: []
failure_fixes: []
related: []
