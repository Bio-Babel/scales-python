# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.rescale_common.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.rescale_common
import_path: scales.rescale_common
kind: function
signature: 'rescale_common(x: ''ArrayLike'', to: ''tuple[float, float]'', from_range:
  ''tuple[float, float]'') -> ''np.ndarray'''
purpose: Linearly rescale *x* from ``from_range`` into ``to``.
description: Linearly rescale *x* from ``from_range`` into ``to``.
parameters:
- name: x
  type: ArrayLike
  required: true
  default: null
- name: to
  type: tuple[float, float]
  required: true
  default: null
- name: from_range
  type: tuple[float, float]
  required: true
  default: null
mutates: ''
returns: np.ndarray
requires: []
writes: []
examples: []
failure_fixes: []
related: []
