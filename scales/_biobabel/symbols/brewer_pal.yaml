# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.brewer_pal.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.brewer_pal
import_path: scales.brewer_pal
kind: function
signature: 'brewer_pal(type: ''str'' = ''seq'', palette: ''Union[int, str]'' = 1,
  direction: ''int'' = 1) -> ''DiscretePalette'''
purpose: ColorBrewer palettes.
description: ColorBrewer palettes.
parameters:
- name: type
  type: str
  required: false
  default: seq
- name: palette
  type: Union[int, str]
  required: false
  default: 1
- name: direction
  type: int
  required: false
  default: 1
mutates: ''
returns: DiscretePalette
requires: []
writes: []
examples:
- "brewer_pal('qual', 'Set1')(5)  # → 5 ColorBrewer hex codes"
- "brewer_pal(palette='RdYlBu', direction=-1)(11)  # → 11 reversed diverging hex codes"
failure_fixes:
- when: Requesting more colors than the palette supports yields None entries.
  suggest:
  - scales.pal_gradient_n
  - scales.pal_viridis
  explanation: ColorBrewer palettes have a fixed maximum (e.g. 9-12); asking for more pads with None. For an arbitrary count, interpolate with pal_gradient_n(brewer_pal(...)(max_n)) or use pal_viridis, which supports any n.
related:
- scales.pal_viridis
- scales.pal_hue
- scales.pal_gradient_n
