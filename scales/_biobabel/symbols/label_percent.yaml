# AUTO-GENERATED tier-1 API surface — signature/params/docstring introspected
# from the installed package (fact, not claim). Regenerate after a version bump:
#   python tools/gen_tier1_symbols.py <import_name>
# Hand-curated tier-2 fields (failure_fixes / anti-patterns / produces) are added
# directly in this file by humans and are NOT overwritten on regeneration unless
# you delete the file first.
# Auto-generated stub for scales.label_percent.
# Review and fill: purpose, mutates, requires, writes, examples, failure_fixes, related.
id: scales.label_percent
import_path: scales.label_percent
kind: function
signature: 'label_percent(accuracy: ''Optional[float]'' = None, scale: ''float'' =
  100, suffix: ''str'' = ''%'', **kwargs: ''Any'') -> ''Callable[[ArrayLike], list[str]]'''
purpose: Label percentages.
description: Label percentages.
parameters:
- name: accuracy
  type: Optional[float]
  required: false
  default: null
- name: scale
  type: float
  required: false
  default: 100
- name: suffix
  type: str
  required: false
  default: '%'
- name: kwargs
  type: Any
  required: true
  default: null
mutates: ''
returns: Callable[[ArrayLike], list[str]]
requires: []
writes: []
examples:
- "label_percent(accuracy=1)([0.15, 0.5, 1.0])  # → ['15%', '50%', '100%']  (scale=100 by default)"
related:
- scales.label_number
- scales.label_dollar
